# 潔沛科技 LINE 群組公告系統 — 安裝與使用說明

這套系統做的事情：每天在網頁（或直接在LINE群組打指令）填「誰、去哪個區域、幾點」，
按下發送後會自動整理成一則公告貼到LINE群組，群組裡的人可以按「確認收到」按鈕回報，
工作做完之後直接在群組留言文字或傳照片，系統會自動記錄成當天的進度回報，
主管可以在網頁後台看到「誰確認了、誰回報了什麼、照片長怎樣」。

整套東西分三個部分，缺一個都不會動：
1. **LINE Bot 帳號**（跟LINE公司申請，免費）— 這一步只有你能做，我沒辦法代辦。
2. **程式本體**（我已經寫好，就是這個資料夾）。
3. **一個24小時開著、能連上網路的地方跑這個程式**（因為LINE的伺服器要能連到我們的程式，放在公司內網電腦不行，一定要能連外網）。這裡建議用 **Render**（一個雲端平台，有免費方案）。

---

## 第一步：申請 LINE Bot 帳號

1. 打開 [LINE Developers Console](https://developers.line.biz/console/)，用你平常用的LINE帳號登入。
2. 建立一個 **Provider**（可以想成「公司」這一層），名稱填「潔沛科技」之類的都可以。
3. 在這個 Provider 下建立一個 **Messaging API channel**（這才是真正的「機器人」）：
   - Channel名稱：例如「潔沛工作公告小幫手」（這會是群組裡看到的機器人名字）
   - 其他欄位照畫面填（公司/服務類別選相近的即可，Email填你的）
4. 建立完後，進入這個 Channel 的設定頁面：
   - **Basic settings** 分頁 → 找到 **Channel secret**，複製起來（等第三步會用到）。
   - **Messaging API** 分頁 →
     - 找到 **Channel access token**，按「Issue」產生一個長效權杖（long-lived），複製起來。
     - **Webhook** 先留著，等第二步部署完、拿到網址後再回來填。

> 這兩個「Channel secret」「Channel access token」都是密碼等級的東西，不要外流、不要貼到LINE群組裡問人。

### 1-1　另外去「LINE Official Account Manager」開啟群組聊天權限（注意：這是另一個網站，不是Developers Console）

這一步很容易找不到，因為設定入口不在剛剛那個Developers Console網站，而是在管理官方帳號用的 [LINE Official Account Manager](https://manager.line.biz/)（用同一個LINE帳號登入即可）：

1. 登入後選擇你剛剛申請的那個帳號（例如「潔沛工作公告小幫手」）。
2. 右上角點「⚙️ 設定」（齒輪圖示）→ 左側選單「**帳號設定**」。
3. 找到「**功能切換**」區塊裡的「**加入群組或多人聊天室**」，改選「**接受邀請加入群組或多人聊天室**」，跳出的確認視窗按「**變更**」。
4. 左側選單改點「**回應設定**」，把「**聊天**」這個功能開啟（否則LINE可能會用預設的罐頭自動回覆擋掉我們自己程式的訊息）。

做完這一步之後，才能把這個帳號邀請加入你們現有的LINE群組。

---

## 第二步：把程式部署到網路上（用 Render，免費）

Render 的免費方案有個要注意的地方：**沒有人用的時候它會自動休眠，休眠後第一次有人叫它會延遲十幾秒到一分鐘才醒來**。這對「每天發一次公告、偶爾回報」這種用法通常還OK，但如果之後覺得太慢或想要更穩定，可以升級Render的付費方案（約每月US$7），或改放在公司自己一台24小時開著、有固定對外網路的電腦上（需要IT協助設定路由器）。

### 2-1　先把程式碼放到 GitHub（不需要會打指令，用網頁操作就好）

1. 到 [github.com](https://github.com) 註冊一個帳號（免費）。
2. 右上角「+」→「New repository」，名稱填 `line-announcement`，設成 **Private**（私人），建立。
3. 進入這個新的repository頁面，點「uploading an existing file」（或 Add file → Upload files）。
4. 把 `LINE群組公告系統` 這個資料夾**裡面的所有檔案**（server.js、package.json、public資料夾、.gitignore）拖進去上傳、按 Commit。
   - **不要**上傳 `db` 資料夾（裡面是本機測試資料，而且 `.gitignore` 已經把它排除了）。

### 2-2　在 Render 建立服務

1. 到 [render.com](https://render.com) 註冊帳號，選「用 GitHub 登入」最方便，順便會自動授權連結你的GitHub。
2. Dashboard → 「New +」→「Web Service」。
3. 選你剛剛建立的 `line-announcement` repository。
4. 設定：
   - **Name**：隨意，例如 `jepe-line-bot`
   - **Region**：Singapore（離台灣近）
   - **Branch**：main
   - **Build Command**：留空或 `npm install`
   - **Start Command**：`node server.js`
   - **Instance Type**：Free
5. 按「Create Web Service」，等它跑完部署（畫面上會顯示log，跑到出現 `LINE 群組公告系統已啟動` 就代表成功了）。
6. 部署完成後，畫面上方會顯示一個網址，長得像：
   `https://jepe-line-bot.onrender.com`
   這就是你的系統網址，之後要打開後台網頁、設定LINE Webhook都會用到它。

### 2-3　回到 LINE Developers Console 設定 Webhook

1. 回到第一步建立的 Channel → **Messaging API** 分頁。
2. **Webhook URL** 填：`https://你的網址.onrender.com/webhook`（結尾一定要有 `/webhook`）
3. 按「Verify」測試，這時候應該會失敗（顯示401），**這是正常的**，因為還沒把Channel secret / access token填進系統。先繼續下一步。
4. 把 **「Use webhook」（使用webhook）** 打開。

---

## 第三步：打開後台網頁，完成最後設定

1. 瀏覽器打開你的系統網址，例如 `https://jepe-line-bot.onrender.com`。
2. 在「① LINE Bot 設定」欄位，把第一步拿到的 **Channel Access Token** 和 **Channel Secret** 貼上、按「儲存設定」。
3. 回到 LINE Developers Console，這次按「Verify」應該就會成功了。
4. 用手機LINE把這個機器人加為好友（Channel頁面上有QR Code），然後把它加入你要公告的工作群組。
5. 在群組裡隨便發一句話（例如「測試」），回到後台網頁「② 公告群組」欄位重新整理一下，應該就會看到這個群組出現，選好後按「設為公告群組」。

設定到這裡就全部完成，可以開始用了！

---

## 平常怎麼用

### 網頁後台（給主管用）
- 打開系統網址，選日期、填「姓名／區域／時間」按新增，重複填完所有人。
- 按「📤 發送今日公告到群組」，群組就會收到今天的工作分配卡片。
- 頁面下方「④ 確認與回報狀況」會自動每5秒更新，看誰按了確認、誰回報了進度／照片。

### 直接在LINE群組打指令（也可以，不一定要開網頁）
- `/指派 王小明 A區 09:00` → 新增一筆分配（可以一次貼多行，一行一筆）
- `/公告` → 立刻把今天的分配整理成卡片發到群組
- `/進度` 或 `/查詢` → 機器人回覆目前確認與回報狀況
- `/說明` → 顯示這些指令的說明

### 員工端
- 收到公告卡片後按「✅ 確認收到」。
- 工作做完後，**直接在群組裡打字說明進度，或直接傳照片**，系統就會自動記錄下來，不需要任何特殊格式或指令。

---

## 之後可能會遇到的狀況

- **免費方案休眠延遲**：如果太久沒人用，Bot可能要等十幾秒到一分鐘才會回應第一則訊息，之後就會恢復正常速度。
- **重新部署會不會遺失資料**：Render免費方案的硬碟不是完全穩定保存的，如果之後修改程式碼重新部署，`db`資料夹（分配紀錄、回報照片）有可能被清空。如果之後這套系統變得很重要、資料不能丟，建議跟我說，我們可以升級成有「持久化硬碟」的付費方案，或改成資料庫服務。
- **想換人負責/換機器人名字**：都在LINE Developers Console裡改，不影響程式本身。
